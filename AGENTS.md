# Mock Platform Repository Rules

本文件定义仓库级公共规则，适用于所有代码、配置、脚本、文档和自动化代理。`MUST` 为强制要求，`SHOULD` 为默认要求；偏离时必须说明理由并获得评审确认。

## 1. 规则优先级

- 用户明确要求 > 本文件 > 距离目标文件最近的子模块 `AGENTS.md` > 其他约定。
- 子模块规则只能补充本模块差异，不得削弱本文件中的安全、权限、数据一致性、测试和交付要求。
- 需求不明确且会改变业务行为、数据语义、安全边界或兼容性时，必须先确认；普通实现细节遵循现有模式。

## 2. 子模块治理

- 每个新建的 Maven/Gradle 子模块、独立部署服务、SDK、前端应用或基础设施模块，必须同时创建本模块的 `AGENTS.md`。
- 新模块缺少 `AGENTS.md` 视为未完成，不得进入主分支。
- 子模块 `AGENTS.md` 只写增量信息：职责与边界、允许的依赖方向、主要入口、构建/测试命令、模块特有约束和验收方式；不要复制根规则。
- 规则冲突时执行更严格的一条，并在变更说明中指出冲突。

## 3. 变更原则

- 只实现已确认的需求，保持最小必要改动；禁止顺带重构、改名、全局格式化或引入推测性能力。
- 优先复用现有模块、接口和模式；新增抽象必须有至少两个真实使用场景。
- 不得覆盖、删除或回退不属于当前任务的改动。
- 公共 API、事件、数据库结构和配置默认保持向后兼容；破坏性变更必须有版本方案、迁移计划和回滚方案。
- 涉及系统边界、核心依赖、数据模型、安全、并发、部署拓扑的决策必须记录 ADR。

## 4. 架构边界

- 模块职责单一，依赖方向清晰；禁止循环依赖、跨层直连数据库或绕过领域服务修改状态。
- MySQL 保存需要恢复的权威业务状态；Redis、配置中心和本地缓存只能作为可重建投影，不得反向覆盖权威状态。
- 跨存储发布使用事务 Outbox 或等价的可恢复机制，禁止依赖不可恢复的双写顺序。
- Runtime 不得访问真实第三方；只有受控 Callback Worker 可以按白名单出站。
- 任何绕过既定边界的临时实现都必须被拒绝，而不是以 TODO 形式进入生产路径。

## 5. Java 与依赖

- 根构建配置统一定义允许使用的 JDK、Spring、构建插件和依赖版本；子模块只能从允许列表选择目标版本，不得自行引入未登记版本。
- 依赖必须集中管理并锁定明确版本；禁止动态版本、未经评估的 Snapshot 和重复能力库。
- 保持代码可读、类型明确、职责聚合；避免超大类、隐式全局状态和无业务价值的通用工具层。
- 异常必须保留原因并转换为稳定错误语义；禁止空 catch、吞异常或直接向客户端暴露堆栈。
- 时间、随机数和外部 I/O 在需要确定性测试的逻辑中必须可替换。

## 6. 数据、事务与并发

- 事务边界必须覆盖完整业务不变量；事务内禁止远程调用、DNS、文件上传等不可控 I/O。
- 幂等不能只靠内存判断，必须由稳定幂等键、数据库唯一约束或等价持久化机制保证。
- 多行锁必须定义并遵守全局固定顺序；后台任务必须使用租约和 fencing token 防止旧 Worker 提交。
- 重试必须有上限、退避和幂等依据；不确定是否已产生副作用时不得自动重复危险操作。
- 数据库迁移必须版本化、可审查，并经过升级和滚动发布验证；普通版本迁移发布后禁止修改，Repeatable Migration 仅用于明确适用的对象。禁止在应用启动时执行不可控的大表变更。
- 金额、时间、时区、枚举和状态机必须使用明确类型与边界，禁止依赖数据库或框架隐式转换。

## 7. API 与契约

- API、事件和配置必须有明确 Schema、版本、必填项、错误码、幂等语义和兼容策略。
- 所有外部输入都在系统边界完成格式、长度、范围和权限校验；内部服务不得信任客户端声明的身份或租户。
- 返回稳定的业务错误码和 `traceId/requestId`；不得泄露内部路径、SQL、凭证或堆栈。
- 发布后的契约和版本对象不可原地修改；变更通过新版本、校验、审批和发布流程生效。

## 8. 安全与合规

- 认证、授权、租户隔离和资源归属校验必须在服务端执行，任何变更不得移除或弱化现有保护。
- Token、Cookie、密钥、签名、个人信息和完整业务报文不得进入源码、普通配置、日志、异常或测试快照。
- 敏感数据必须按公司规范加密、脱敏、最小化采集并设置保留期限。
- SQL、模板、表达式和命令必须使用安全 API；禁止字符串拼接执行不可信输入。
- 外部请求默认使用 HTTPS，并执行目标白名单、DNS/IP 校验、超时和响应大小限制；HTTP 仅允许在明确批准的非生产环境及目标白名单内使用，生产环境禁止明文 HTTP。禁止自动跟随到未授权目标。
- 安全策略缺失、过期、签名错误或无法确认时必须 fail-closed，并产生可观测告警。

## 9. 测试与质量门禁

- 行为变更必须有自动化测试；Bug 修复应先提供可复现的回归用例。
- 至少覆盖成功、失败、边界、权限、幂等和恢复路径；并发、锁、状态机和安全逻辑必须有针对性测试。
- 集成边界使用契约测试；禁止只用 Mock 证明数据库约束、序列化、事务或真实客户端兼容性。
- 不得通过删除测试、降低断言、扩大忽略范围或降低质量阈值使流水线通过。
- 提交前运行最小且充分的模块测试、静态检查和构建；具体命令由子模块 `AGENTS.md` 定义。

## 10. 可观测性与审计

- 日志采用结构化字段并贯通 `traceId/requestId`；错误日志应包含可定位上下文，但不得包含敏感数据。
- 核心路径必须提供延迟、吞吐、错误、拒绝、重试、积压和资源使用指标。
- 发布、回滚、审批、权限、安全策略和人工状态变更必须记录不可抵赖审计。
- 告警必须可行动，包含影响范围、当前状态和排查入口；禁止用高频无效日志代替告警。

## 11. Git 与交付

- 保持提交单一目的、差异可审查；生成文件、IDE 文件、凭证和本地环境产物不得提交。
- 未经用户明确要求，不得创建分支、提交、推送、合并、开 PR、删除分支或改写历史。
- 合并前必须完成代码评审和必需的 CI 门禁；高风险安全、事务、并发和迁移变更至少需要相应领域评审。
- 完成任务前必须检查实际 diff，移除本任务引入的无关改动，并报告变更文件、实现行为、验证结果和未验证项。

## 12. 完成标准

一项工作只有同时满足以下条件才算完成：

- 需求与验收标准已满足，且没有扩大范围；
- 架构边界、权限、安全和数据不变量未被破坏；
- 相关测试、静态检查和构建通过；
- 文档、Schema、迁移和运行手册已随行为同步更新；
- 不存在未说明的风险、跳过项或临时生产逻辑。
