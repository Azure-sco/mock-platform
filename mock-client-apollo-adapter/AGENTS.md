# Apollo adapter module

- Java 8. It adapts configuration input only and must not redefine routing rules.
- M0 provides an explicit local bridge; do not claim production Apollo integration.
- Verify with `mvn -pl mock-client-apollo-adapter -am test`.
