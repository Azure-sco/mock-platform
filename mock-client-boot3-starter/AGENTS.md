# Boot 3 starter

- Java 17 and Spring Boot 3 only.
- Transport adapters delegate policy decisions to `mock-client-core`.
- Verify with `mvn -pl mock-client-boot3-starter -am test` using the JDK 17 toolchain.
