# Client core module

- Java 8, pure Java, and independent of Spring/Feign/Control/Runtime.
- Owns shared routing, failure, context, URL, and header rules.
- Verify with `mvn -pl mock-client-core -am test`.
