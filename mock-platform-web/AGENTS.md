# Web console

- Vue 3 + TypeScript + Vite + Element Plus.
- Phase 0 contains only the application shell, health dashboard, stores, HTTP errors, and 404.
- Verify with `npm run type-check`, `npm run lint`, and `npm run build`.
