# JDK 17 sample

- Java 17 / Boot 3 compatibility harness for Feign and RestTemplate.
- Keep business behavior deliberately small; tests prove SDK compatibility.
- Verify with `mvn -pl mock-sample-jdk17 -am verify` using the JDK 17 toolchain.
