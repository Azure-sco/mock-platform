# JDK 8 sample

- Java 8 / Boot 2 compatibility harness for Feign and RestTemplate.
- Keep business behavior deliberately small; tests prove SDK compatibility.
- Verify with `mvn -pl mock-sample-jdk8 -am verify` using the JDK 8 toolchain.
